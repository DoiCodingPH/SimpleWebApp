@model List<Post>

<div>
    <ul>
        @foreach (var post in Model)
        {
        <li>
            <a asp-controller="Home" asp-action="Post" asp-route-id="@post.Id">View</a>
            <a>@post.Title</a>
        </li>
        }
    </ul>
</div>